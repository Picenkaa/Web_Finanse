<?xml version='1.0' encoding='UTF-8' ?>
<!-- was: <?xml version="1.0" encoding="UTF-8"?> -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
       http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">

    <bean class="org.springframework.web.servlet.mvc.support.ControllerClassNameHandlerMapping"/>

    <!--
    Most controllers will use the ControllerClassNameHandlerMapping above, but
    for the index controller we are using ParameterizableViewController, so we must
    define an explicit mapping for it.
    -->
    <bean id="urlMapping" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <prop key="index.htm">indexController</prop> <!-- Index failo mappingas -->
                <prop key="/category/*">kategorijos</prop> <!-- Tam, kad būtų galima pasiekti kontroleryje aprašytus metodus, nurodome, kad mes naudosime kelią /category/*. *
                                                            reiškia, kad visi sub path nurodyti kontroleryje bus pasiekiami. Praeitą kartą paskaitos metu nesigavo, nes trūko * ir jis pasiekė 
                                                            tik vieną konkretų metodą. Šioje vietoje nurorome url kaip pasieksime savo konrolerį ir jo metodus.-->
                <prop key="klaida.htm">klaida</prop> <!-- Prisidedame dar vieną puslapį, į kurį mus numes klaidos atveju -->
            </props>
        </property>
    </bean>

    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver"
          p:prefix="/WEB-INF/jsp/"
          p:suffix=".jsp" />

    <!--
    The index controller.
    -->
    <bean name="indexController"
          class="org.springframework.web.servlet.mvc.ParameterizableViewController"
          p:viewName="index" />
    <!--
    Kategorijų kontroleris.
        name/id turi sutapti su <prop> nurodytu pavadinimu. Šiuo atveju yra kategorijos.
        class nurodome, kuri mūsų klasė yra kontroleris.
        Jei bus keli kontroleriai, kiekvienam jų aprašome mapping ir ir <prop>
    -->
    <bean name="kategorijos"
          class="ctrl.MainControl"/>
    <!-- Klaidos kontroleris -->
    <bean class="org.springframework.web.servlet.mvc.ParameterizableViewController" name="klaida" p:viewName="klaida"/>
</beans>
